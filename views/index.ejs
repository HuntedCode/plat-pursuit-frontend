<!-- Breadcrumb -->
 <%- include('partials/breadcrumb', { items: [{ text: 'Home', url: '/' }, { text: 'Dashboard' }] }) %>

<div class="hero min-h-[50vh] bg-base-200">
    <div class="hero-content text-center">
        <div class="max-w-md">
            <h1 class="text-5xl font-bold">Welcome, <%= data.user.name %>!</h1>
            <p class="py-6">You've earned <%= data.user.trophiesEarned %> trophies. Keep tracking!</p>
            <button class="btn btn-primary">View Games</button>
        </div>
    </div>
</div>